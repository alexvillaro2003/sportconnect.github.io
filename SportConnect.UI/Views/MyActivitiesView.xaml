<UserControl x:Class="SportConnect.UI.Views.MyActivitiesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid>
        <!-- Loading -->
        <TextBlock Text="Loading..."
                   HorizontalAlignment="Center" VerticalAlignment="Center"
                   Foreground="{StaticResource TextSecondaryBrush}"
                   Visibility="{Binding IsBusy, Converter={StaticResource NullToVisibilityConverter}}" />

        <!-- Error -->
        <TextBlock Text="{Binding ErrorMessage}"
                   HorizontalAlignment="Center" VerticalAlignment="Center"
                   Foreground="{StaticResource ErrorBrush}"
                   TextWrapping="Wrap" MaxWidth="400"
                   Visibility="{Binding ErrorMessage, Converter={StaticResource NullToVisibilityConverter}}" />

        <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="4,4,4,0">
            <ScrollViewer.Style>
                <Style TargetType="ScrollViewer">
                    <Setter Property="Visibility" Value="Visible" />
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsBusy}" Value="True">
                            <Setter Property="Visibility" Value="Collapsed" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </ScrollViewer.Style>

            <StackPanel>
                <!-- Empty state -->
                <TextBlock Text="You haven't joined any activities yet."
                           HorizontalAlignment="Center" Margin="0,40,0,0"
                           Foreground="{StaticResource TextSecondaryBrush}"
                           FontSize="14">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Collapsed" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding MyActivities.Count}" Value="0">
                                    <Setter Property="Visibility" Value="Visible" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>

                <ItemsControl ItemsSource="{Binding MyActivities}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Button Command="{Binding DataContext.OpenDetailsCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                    CommandParameter="{Binding}"
                                    Padding="0"
                                    HorizontalContentAlignment="Stretch"
                                    HorizontalAlignment="Stretch">
                                <Button.Template>
                                    <ControlTemplate TargetType="Button">
                                        <ContentPresenter HorizontalAlignment="Stretch" />
                                    </ControlTemplate>
                                </Button.Template>

                                <Border Style="{StaticResource ActivityCard}">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>

                                        <!-- Sport icon -->
                                        <Grid Grid.Column="0"
                                              Width="48" Height="48"
                                              VerticalAlignment="Center"
                                              Margin="0,0,16,0">
                                            <Ellipse Fill="{Binding Sport.Name,
                                                             Converter={StaticResource SportToColorConverter}}" />
                                            <TextBlock Text="{Binding Sport.Name,
                                                               Converter={StaticResource StringToInitialConverter}}"
                                                       HorizontalAlignment="Center"
                                                       VerticalAlignment="Center"
                                                       Foreground="White"
                                                       FontWeight="Bold"
                                                       FontSize="18" />
                                            <Ellipse>
                                                <Ellipse.Fill>
                                                    <ImageBrush ImageSource="{Binding Sport.Name,
                                                                    Converter={StaticResource SportToImageConverter}}"
                                                                Stretch="UniformToFill" />
                                                </Ellipse.Fill>
                                            </Ellipse>
                                        </Grid>

                                        <!-- Main info -->
                                        <StackPanel Grid.Column="1"
                                                    VerticalAlignment="Center"
                                                    Margin="0,0,16,0">
                                            <TextBlock Text="{Binding Sport.Name}"
                                                       FontSize="16" FontWeight="Bold"
                                                       Foreground="{StaticResource TextPrimaryBrush}" />
                                            <TextBlock Text="{Binding Location}"
                                                       Margin="0,4,0,0" FontSize="13"
                                                       Foreground="{StaticResource TextSecondaryBrush}" />
                                            <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                                                <TextBlock Text="{Binding ScheduledAt, StringFormat='{}{0:dd MMM}'}"
                                                           FontSize="12"
                                                           Foreground="{StaticResource TextSecondaryBrush}" />
                                                <TextBlock Text="  ·  " FontSize="12"
                                                           Foreground="{StaticResource TextSecondaryBrush}" />
                                                <TextBlock Text="{Binding ScheduledAt, StringFormat='{}{0:HH:mm}'}"
                                                           FontSize="12"
                                                           Foreground="{StaticResource TextSecondaryBrush}" />
                                                <TextBlock Text="  ·  " FontSize="12"
                                                           Foreground="{StaticResource TextSecondaryBrush}" />
                                                <TextBlock Text="{Binding Creator.Username, StringFormat='by {0}'}"
                                                           FontSize="12"
                                                           Foreground="{StaticResource TextSecondaryBrush}" />
                                            </StackPanel>
                                        </StackPanel>

                                        <!-- Spots + status badge -->
                                        <StackPanel Grid.Column="2"
                                                    VerticalAlignment="Center"
                                                    HorizontalAlignment="Center">
                                            <TextBlock Text="{Binding MaxPlayers, StringFormat='{}{0} spots'}"
                                                       HorizontalAlignment="Center"
                                                       FontWeight="Bold" FontSize="18"
                                                       Foreground="{StaticResource TextPrimaryBrush}" />
                                            <Border x:Name="StatusBadge"
                                                    Background="#DCFCE7"
                                                    CornerRadius="12"
                                                    Padding="8,4"
                                                    HorizontalAlignment="Center"
                                                    Margin="0,6,0,0">
                                                <TextBlock x:Name="StatusBadgeText"
                                                           Text="Open"
                                                           FontSize="11" FontWeight="SemiBold"
                                                           Foreground="#15803D"
                                                           HorizontalAlignment="Center" />
                                            </Border>
                                        </StackPanel>
                                    </Grid>
                                </Border>
                            </Button>

                            <DataTemplate.Triggers>
                                <DataTrigger Binding="{Binding IsFull}" Value="True">
                                    <Setter TargetName="StatusBadge" Property="Background" Value="#FEE2E2" />
                                    <Setter TargetName="StatusBadgeText" Property="Text" Value="Full" />
                                    <Setter TargetName="StatusBadgeText" Property="Foreground" Value="#B91C1C" />
                                </DataTrigger>
                            </DataTemplate.Triggers>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
