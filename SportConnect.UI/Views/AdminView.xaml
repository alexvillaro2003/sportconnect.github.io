<UserControl x:Class="SportConnect.UI.Views.AdminView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:enums="clr-namespace:SportConnect.Domain.Enums;assembly=SportConnect.Domain">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header: title + subtitle + search -->
        <Grid Grid.Row="0" Margin="0,0,0,20">
            <StackPanel VerticalAlignment="Center">
                <TextBlock Text="User management"
                           FontSize="20" FontWeight="Bold"
                           Foreground="{StaticResource TextPrimaryBrush}" />
                <TextBlock Text="{Binding Users.Count, StringFormat='{}{0} users registered'}"
                           Foreground="{StaticResource TextSecondaryBrush}"
                           FontSize="13" Margin="0,4,0,0" />
            </StackPanel>

            <!-- Rounded search box -->
            <TextBox Text="{Binding SearchQuery, UpdateSourceTrigger=PropertyChanged}"
                     Width="280" HorizontalAlignment="Right" VerticalAlignment="Center"
                     Tag="Search by username or email..."
                     Background="{StaticResource SurfaceBrush}">
                <TextBox.Template>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{StaticResource BorderBrush}"
                                BorderThickness="1"
                                CornerRadius="8">
                            <Grid>
                                <TextBlock x:Name="Placeholder"
                                           Text="{TemplateBinding Tag}"
                                           Foreground="{StaticResource TextSecondaryBrush}"
                                           Margin="12,8"
                                           VerticalAlignment="Center"
                                           Visibility="Collapsed" />
                                <ScrollViewer x:Name="PART_ContentHost"
                                              Margin="12,8"
                                              VerticalAlignment="Center" />
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="Text" Value="">
                                <Setter TargetName="Placeholder" Property="Visibility" Value="Visible" />
                            </Trigger>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource PrimaryBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </TextBox.Template>
            </TextBox>
        </Grid>

        <!-- Error -->
        <TextBlock Grid.Row="1"
                   Text="{Binding ErrorMessage}"
                   Foreground="{StaticResource ErrorBrush}"
                   TextWrapping="Wrap"
                   Visibility="{Binding ErrorMessage, Converter={StaticResource NullToVisibilityConverter}}"
                   Margin="0,0,0,8" />

        <!-- DataGrid -->
        <DataGrid Grid.Row="2"
                  Style="{StaticResource ModernDataGrid}"
                  ItemsSource="{Binding FilteredUsers}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  IsReadOnly="True">

            <DataGrid.Columns>

                <DataGridTextColumn Header="Username"
                                    Binding="{Binding Username}"
                                    Width="*" />

                <DataGridTextColumn Header="Email"
                                    Binding="{Binding Email}"
                                    Width="2*" />

                <DataGridTextColumn Header="Role"
                                    Binding="{Binding Role}"
                                    Width="80" />

                <DataGridTextColumn Header="Joined"
                                    Binding="{Binding CreatedAt, StringFormat=\{0:dd/MM/yyyy\}}"
                                    Width="100" />

                <!-- Actions column -->
                <DataGridTemplateColumn Header="Actions" Width="220">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal"
                                        VerticalAlignment="Center">

                                <!-- Delete pill -->
                                <Button Content="Delete"
                                        Command="{Binding DataContext.DeleteUserCommand,
                                                  RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        CommandParameter="{Binding}"
                                        Cursor="Hand"
                                        Margin="0,0,8,0">
                                    <Button.Template>
                                        <ControlTemplate TargetType="Button">
                                            <Border x:Name="Bd"
                                                    Background="#FEF2F2"
                                                    CornerRadius="6"
                                                    Padding="12,6">
                                                <TextBlock Text="{TemplateBinding Content}"
                                                           Foreground="{StaticResource ErrorBrush}"
                                                           FontSize="12" FontWeight="SemiBold"
                                                           HorizontalAlignment="Center"
                                                           VerticalAlignment="Center" />
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="Bd" Property="Background" Value="#FEE2E2" />
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Button.Template>
                                </Button>

                                <!-- Make Admin / Make User pill -->
                                <Button Command="{Binding DataContext.ToggleRoleCommand,
                                                  RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        CommandParameter="{Binding}"
                                        Cursor="Hand">
                                    <Button.Template>
                                        <ControlTemplate TargetType="Button">
                                            <Border x:Name="Bd"
                                                    Background="#EFF6FF"
                                                    CornerRadius="6"
                                                    Padding="12,6">
                                                <TextBlock Text="{TemplateBinding Content}"
                                                           Foreground="{StaticResource PrimaryBrush}"
                                                           FontSize="12" FontWeight="SemiBold"
                                                           HorizontalAlignment="Center"
                                                           VerticalAlignment="Center" />
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="Bd" Property="Background" Value="#DBEAFE" />
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Button.Template>
                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Setter Property="Content" Value="Make Admin" />
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Role}"
                                                             Value="{x:Static enums:UserRole.Admin}">
                                                    <Setter Property="Content" Value="Make User" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>

                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

            </DataGrid.Columns>
        </DataGrid>

    </Grid>
</UserControl>
